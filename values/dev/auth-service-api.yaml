replicaCount: 1

image:
  repository: 672061768993.dkr.ecr.ap-southeast-1.amazonaws.com/auth-service-api

service:
  port: 80
  targetPort: 8080

env:
  APP_ENV: dev
  EMAIL_SERVICE_URL: http://email-service-api.default.svc.cluster.local
  # TODO: confirm the real dev portal URL (this is a guess based on the
  # localhost origin already in CORS_ALLOWED_ORIGINS) and replace it.
  PASSWORD_RESET_URL_BASE: http://localhost:3020/reset-password
