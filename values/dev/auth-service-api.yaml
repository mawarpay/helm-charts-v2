replicaCount: 1

image:
  repository: 672061768993.dkr.ecr.ap-southeast-1.amazonaws.com/auth-service-api

service:
  port: 80
  targetPort: 8080

env:
  APP_ENV: dev
  EMAIL_SERVICE_URL: http://email-service-api.default.svc.cluster.local
  # Confirmed via values/dev/portal-admin-vue.yaml ingress host (dev pair of
  # the prod portal.mawarpay.com shown in the reset-password screenshot).
  PASSWORD_RESET_URL_BASE: https://portal-dev.mawarpay.com/reset-password
